# Current Unimplemented Scope

Date: 2026-06-27

Source docs:

- `spec.md`
- `README.md`
- `artifacts/current-implementation-completion-audit-2026-06-28.md`
- `artifacts/v1-readiness-summary.md`
- `artifacts/active-non-oauth-buildout-todos.md`
- `artifacts/current-implementation-todos-oauth-parked.md`

## Newly Closed

- Remote plugin package signature verification is implemented and locally proven under `artifacts/tranche-plugin-package-signature-verification/`.
- `goatshot.plugin-registry.v1` now supports optional RSA SHA-256 signature metadata through `signature`, `signatureAlgorithm`, and `signaturePublicKeyPem`.
- Valid signatures are verified before staging; invalid signatures block staging; unsigned registry entries remain checksum-only with a warning.
- Current formal release evidence after the Google Photos adapter tranche is saved under `artifacts/tranche-release-proof-after-google-photos-adapter-2026-06-28/`: Release build passed with 0 warnings and 0 errors, full Release tests passed 576 tests, CLI help/diagnostics/package proof passed, and the formal release proof bundle command reported 164 included files with 0 policy exclusions. Previous YouTube/OneNote adapter proof remains available under `artifacts/tranche-release-proof-after-youtube-onenote-adapters-2026-06-28/`, previous completion-audit proof remains available under `artifacts/tranche-release-proof-after-completion-audit-2026-06-28/`, previous browser optional-lane closure proof remains available under `artifacts/tranche-release-proof-after-browser-optional-lane-closure-2026-06-27/`, earlier hardware evidence recorder release proof remains available under `artifacts/tranche-release-proof-after-hardware-evidence-recorder/`, earlier required desktop evidence recorder release proof remains available under `artifacts/tranche-release-proof-after-required-desktop-evidence-recorder/`, earlier clean-machine evidence recorder release proof remains available under `artifacts/tranche-release-proof-after-clean-machine-evidence-recorder/`, earlier manual proof summary release proof remains available under `artifacts/tranche-release-proof-after-manual-proof-summaries/`, earlier desktop summary release proof remains available under `artifacts/tranche-release-proof-after-manual-desktop-proof-summary/`, earlier plugin update task lifecycle proof remains available under `artifacts/tranche-release-proof-after-plugin-update-task-lifecycle/`, earlier plugin scheduler handoff proof remains available under `artifacts/tranche-release-proof-after-plugin-update-scheduler-handoff/`, earlier reviewed plugin activation proof remains available under `artifacts/tranche-release-proof-after-plugin-reviewed-activation/`, earlier manual-validation handoff proof remains available under `artifacts/tranche-release-proof-after-current-manual-handoff/`, and earlier diagnostics/video truth proof remains available under `artifacts/tranche-release-proof-after-diagnostics-video-truth/`.
- The release build no longer reports the `SQLitePCLRaw.lib.e_sqlite3` 2.1.11 high-severity vulnerability warning; the app now pins `SQLitePCLRaw.bundle_e_sqlite3` 3.0.3 and `dotnet list ... --vulnerable` reports no vulnerable packages for `GoatShot.App`.
- Local post-recording audio denoise export is implemented through `goatshot video denoise` and locally proven under `artifacts/tranche-video-audio-denoise/`.
- Installer proof readiness verification is implemented through `scripts\verify-installer-package.ps1` and locally proven under `artifacts/tranche-installer-proof-readiness/`; the current machine still lacks Inno Setup and no compiled installer artifact exists.
- Governed plugin background update checks and stage-only runs are implemented through `goatshot plugins background-updates --registry <registry> [--mode check-only|stage-only]` and locally proven under `artifacts/tranche-plugin-background-updates/`. This does not automatically install, trust, enable, allowlist, or execute plugin code.
- Plugin update Task Scheduler handoff generation is implemented through `goatshot plugins schedule-updates --registry <registry> [--mode check-only|stage-only] [--interval-hours 24] [--output <folder>]` and locally proven under `artifacts/tranche-plugin-update-scheduler-handoff/`. It writes a manifest plus run/register/unregister scripts, rejects sensitive registry URLs unless explicitly accepted, does not register a task by itself, and does not install, trust, enable, allowlist, or execute plugin code.
- Plugin update Task Scheduler lifecycle commands are implemented through `goatshot plugins update-task <status|register|unregister> --manifest <plugin-update-schedule.json>` and locally proven under `artifacts/tranche-plugin-update-task-lifecycle/`. Status queries Task Scheduler, registration requires `--accept-task-registration`, removal requires `--accept-task-removal`, dry-run proves register/unregister wiring without mutating Task Scheduler, and all command output is reported with redaction. The proof did not create a scheduled task and still did not install, trust, enable, allowlist, or execute plugin code.
- Reviewed local-plugin activation metadata writes are implemented through `goatshot plugins activate <plugin-id> --trust --enable --enable-local-plugins (--allow-action <action-id>|--allow-all-actions) --accept-risk` and locally proven under `artifacts/tranche-plugin-reviewed-activation/`. Activation writes only settings metadata, honors managed policy, and does not run plugin code.
- Browser extension enterprise policy template planning is implemented through `goatshot browser-extension enterprise-policy-plan` and locally proven under `artifacts/tranche-browser-extension-enterprise-policy-plan/`. This does not apply policy, install extensions, register native hosts, contact MDM/store services, or prove managed deployment.
- OAuth/live-account proof planning is implemented through `goatshot oauth live-proof-plan` and locally proven under `artifacts/tranche-oauth-live-proof-plan/`. This does not open a browser, contact providers, exchange codes, store tokens, refresh tokens, upload files, or prove live accounts.
- Provider-specific OAuth proof-plan polish is implemented and locally proven under `artifacts/tranche-oauth-provider-specific-proof-plan/` and the current focused source/test lane. The generated Google Drive, Google Photos, OneDrive, Dropbox, YouTube, and OneNote plans include provider kind, scope review, consent checklist, account diagnostics, provider-specific upload command hints, cleanup boundaries, and false mutation flags. This still does not perform live OAuth, upload, cleanup, or account proof.
- OAuth live-evidence recording is implemented through `goatshot oauth record-live-evidence` and locally proven under `artifacts/tranche-oauth-live-evidence-recorder/`. A passed record requires consent, exchange, refresh, upload, cleanup, and account evidence references, while the recorder itself does not open a browser, contact providers, exchange codes, store tokens, refresh tokens, upload files, or delete remote files.
- Google Photos upload, YouTube upload, and OneNote export are implemented as executable OAuth share-provider adapters, with Settings fields, DPAPI token storage, provider diagnostics, workflow-profile import/export, OAuth proof-plan/evidence classification, and focused fake-HTTP tests. This closes the current optional/source-level roadmap-provider gap; live consent, upload, cleanup, and account proof remain manual.
- Deterministic foreground mask generation is implemented through `goatshot video generate-mask` and locally proven under `artifacts/tranche-foreground-mask-generation/`. The proof uses a synthetic green-screen clip, generates a keyed foreground mask, and feeds that mask into the reviewed background-replacement flow. This is local FFmpeg mask generation, not model-grade AI/person segmentation.
- Companion portal local media review is implemented through `goatshot companion-portal media-review --media <file> --accept-media-copy` and locally proven under `artifacts/tranche-companion-portal-media-review/`. The export/serve paths also accept repeatable `--media` only with explicit copy acceptance, copy only selected files into static local review pages, omit source paths, and keep portal contact, sync, upload, remote clients, secret access, policy mutation, plugin execution, account login, and write routes false.
- Companion portal explicit self-hosted shared-token preview is implemented through `goatshot companion-portal serve --host 0.0.0.0 --self-hosted --accept-remote-clients --auth-token-env <name> [--check]` and locally proven under `artifacts/tranche-companion-portal-self-hosted-auth/`. It remains read-only, requires explicit remote-client acceptance plus a token, returns `401` without auth, returns `405` for mutating methods, does not serialize tokens into reports, and still does not provide hosted accounts, account login, team sync, hosted media, remote admin, sync, upload, secret access, policy mutation, or plugin execution.
- External person-segmentation mask generation is implemented through `goatshot video person-mask <video> --runner <exe> --runner-args "<template>" --accept-external-runner` and locally proven under `artifacts/tranche-person-segmentation-runner/`. It requires an operator-provided local runner, `{input}` and `{output}` placeholders, explicit external-runner acceptance, optional local `--model`, timeout bounds, and output verification before indexing a `PersonSegmentationMaskVideo`. It does not bundle, trust, enable, register, run, host, or broadly quality-certify a segmentation model and does not contact a hosted AI service.
- Hosted person-segmentation service handoff is implemented through `goatshot video hosted-person-mask <video> --endpoint <url> --accept-hosted-service [--api-key-env NAME]` and locally proven under `artifacts/tranche-hosted-person-segmentation-service/`. It requires explicit source-upload acceptance, sends multipart source media to an operator-supplied HTTP(S) service, supports token lookup from an environment variable, writes the binary mask-video response, and can index `PersonSegmentationMaskVideo`. It does not provide a first-party hosted account, provider certification, model certification, or automatic bundled local inference.
- Stage-only person-segmentation model package acquisition is implemented through `goatshot video person-model validate|stage --manifest <manifest.json> [--accept-download]` and locally proven under `artifacts/tranche-person-segmentation-model-package-staging/`. It validates `goatshot.person-segmentation-model.v1` manifests, accepts local files or explicitly accepted HTTP(S) downloads, verifies schema/id/URI/size/SHA-256, writes a staged model plus stage manifest, and keeps inference, trust, enablement, runner registration, hosted-service contact, and model certification false.
- Still-image and video-frame mask quality evaluation is implemented through `goatshot video mask-quality --generated-mask <png|video> --reference-mask <png|video>` and locally proven under `artifacts/tranche-video-mask-quality-evaluator/` and `artifacts/tranche-video-mask-quality-video-evaluator/`. It compares supplied masks with IoU, Dice, precision, recall, and accuracy thresholds, writes Markdown/JSON evidence, records per-frame video metrics when videos are supplied, and keeps model download, segmentation inference, hosted-service contact, source-media mutation, and whole-model certification flags false.
- Clean-machine proof-kit generation is implemented through `goatshot manual-validation clean-machine-kit --folder <evidence-folder> [--portable-zip <zip>] [--installer <exe>] [--copy-package]` and locally proven under `artifacts/tranche-clean-machine-proof-kit/`. It discovers and hashes the current portable ZIP, writes a VM/operator runbook, script template, manifest, and evidence checklist, and can copy package artifacts only when explicitly requested. It does not perform or certify a clean Windows VM/profile pass, human GUI click-through, compiled installer creation, or installer install/uninstall proof.
- Clean-machine evidence recording is implemented through `goatshot manual-validation record-clean-machine-evidence --folder <evidence-folder> --status passed|failed|blocked|pending` and locally proven under `artifacts/tranche-clean-machine-evidence-recorder/`. A passed record requires machine/profile, package, hash, diagnostics, paths, first-launch, settings, capture/export, installer, and privacy-review evidence references, while the recorder itself does not launch GoatShot, run installers, mutate profiles, capture screens, certify the VM, or update the manual lane.
- Required desktop evidence recording is implemented through `goatshot manual-validation record-desktop-evidence --folder <evidence-folder> --lane keyboard|screen-reader|text-scaling|high-contrast|live-region-drag --status passed|failed|blocked|pending` and locally proven under `artifacts/tranche-required-desktop-evidence-recorder/`. A passed record requires lane-specific reviewed evidence categories, while the recorder itself does not launch GoatShot, change Windows settings, capture or record screens, certify accessibility, mutate profiles, or update manual lane files.
- Manual desktop-proof summary generation is implemented through `goatshot manual-validation desktop-proof --folder <evidence-folder> [--run-commands]` and locally proven under `artifacts/tranche-manual-desktop-proof-summary/`. The desktop proof packet now includes `desktop-proof-summary.md` and `desktop-proof-summary.json` with command counts, failed commands, expected/missing evidence, current-machine accessibility environment fields, remaining human lanes, next operator steps, and explicit claim boundaries. This is a reporting/handoff helper only; it does not complete keyboard traversal, Narrator/NVDA observation, text-scaling, high-contrast, live region-drag, clean-machine GUI proof, or accessibility certification.
- Manual hardware-proof summary generation is implemented through `goatshot manual-validation hardware-proof --folder <evidence-folder> [--run-commands]` and locally proven under `artifacts/tranche-manual-hardware-proof-summary/`. The hardware proof packet now includes `hardware-proof-summary.md` and `hardware-proof-summary.json` with command counts, nonzero diagnostics, expected/missing evidence, current display/FFmpeg environment fields, remaining hardware-gated lanes, next operator steps, and explicit claim boundaries. This is a reporting/handoff helper only; it does not complete live multi-monitor capture, multi-monitor recording, long-run recording stability, Android safe-device media proof, or safe-content review.
- Hardware evidence recording is implemented through `goatshot manual-validation record-hardware-evidence --folder <evidence-folder> --lane multi-monitor-capture|multi-monitor-recording|long-recording|android-safe-device-proof --status passed|failed|blocked|pending` and locally proven under `artifacts/tranche-hardware-evidence-recorder/`. A passed record requires lane-specific reviewed evidence categories, while the recorder itself does not capture or record the desktop, contact Android devices, import phone media, change device settings, certify hardware, mutate profiles, or update manual lane files.
- Browser extension publication evidence recording is implemented through `goatshot browser-extension record-publication-evidence` and locally proven under `artifacts/tranche-browser-publication-evidence-recorder/`. A passed record requires package, account, submission, review, signing, listing, and install evidence references, while the recorder itself does not contact store accounts, upload packages, submit reviews, sign or publish listings, install extensions, mutate browser profiles, register native hosts, or apply enterprise policy.
- Current browser optional-lane closure is recorded under `artifacts/tranche-browser-optional-lane-closure-2026-06-27/` and `artifacts/manual-validation/2026-06-27-current-required-proof/browser-extension-live-fixture/chrome-firefox-current-v1-decision.md`. Browser Extension Live Fixture is `NotApplicable` for the current local V1 claim set, so the generated 2026-06-27 findings now report zero optional compatibility findings. Chrome/Firefox live fixture proof still must be reopened before advertising Chrome/Firefox live behavior.
- Current manual-validation handoff evidence is refreshed under `artifacts/manual-validation/2026-06-27-current-required-proof/` with tranche notes under `artifacts/tranche-current-manual-validation-handoff-2026-06-27/`. Baseline proof passed Release build and 542 Release tests, desktop and hardware proof packets were collected, the required operator pack and clean-machine proof kit were regenerated, and Android/ADB no-device diagnostics were hardened so proof commands return promptly without orphaned ADB output handles. This is current handoff/proof tooling evidence, not completion of the remaining human, hardware, clean-machine, browser-store, OAuth, or hosted/admin lanes.

## Still Not Implemented Or Not Proven

- Source-level roadmap provider adapters: none currently known from the latest scan. Google Photos upload, YouTube upload, and OneNote export moved to executable adapters; their live-account proof remains parked/manual.
- Live OAuth/cloud-account proof: real consent screens, real refresh-token validation, safe upload/cleanup proof, and reviewed live-account evidence remain parked/uncollected.
- Clean-machine and installer proof: proof-kit generation and evidence recording exist, but clean Windows VM/profile proof, human GUI click-through proof, compiled installer creation on a machine with Inno Setup, and installer install/uninstall proof remain manual.
- Desktop accessibility/manual proof: desktop evidence recording exists, but actual keyboard traversal, narrated screen-reader proof, text scaling, high contrast, and live region-drag behavior remain release-blocking required lanes until an operator performs the checks and records the lane results. The 2026-06-27 findings also keep clean-machine portable/installer proof release-blocking until an operator completes or explicitly waives it.
- Browser store publication and deployment proof: evidence recording exists, but actual store account submission, review, signing, listing availability, permanent/store-managed automatic installation, actual enterprise policy deployment/force-install proof, and future Chrome/Firefox live proof remain later/manual lanes.
- Hardware/device proof: the 2026-06-27 hardware packet collected local recording/device/WGC diagnostics, writes a consolidated hardware-proof summary, and now has a reviewed evidence recorder, but actual multi-monitor capture, multi-monitor recording, long recording stability, and Android safe-device proof remain blocked claim boundaries until safe staged hardware/device proof is performed and the lanes are recorded. Production Android live streaming, Android companion-app streaming, and scrcpy-style mirroring remain post-V1 scope.
- Virtual printer driver: GoatShot remains file-drop/import-only; true Windows virtual-printer driver or Print Support App work is deferred until admin/signing/clean-machine constraints are accepted.
- Plugin marketplace expansion: hosted marketplace accounts, ratings, payments, publisher review, hosted trust chain/cert revocation, remote execution, automatic install/trust/enable/allowlist/execute update behavior, and automatic self-registering updater services remain later-scope. Reviewed local activation exists only as an explicit operator command after local manifest/package review, scheduler handoff writes operator-reviewable Task Scheduler scripts, and `plugins update-task` can only query/register/unregister the scheduled task with explicit acceptance.
- Companion portal/team admin: hosted portal accounts, first-class account login, team sync, hosted/remote media hosting, and remote/multi-user admin sync remain later-scope; the implemented self-hosted shared-token preview is only a read-only evidence preview, not account login, team sync, hosted media, or remote admin.
- AI/person mask generation beyond the local runner/hosted-handoff/evaluator/package-staging contract: deterministic foreground mask generation, reviewed mask/matte webcam-background export, explicit local external-runner person masks, explicit hosted service handoff, stage-only local/remote model package acquisition, and still-image/video-frame mask quality evaluation are implemented, but bundled model inference, first-party hosted segmentation account proof, automatic model inference, and broad model certification remain later-scope.

## Current Boundary

Most remaining items are proof-gated, account-gated, hardware-gated, admin-gated, or intentionally post-V1. The locally buildable gaps closed in this pass were remote plugin package signature verification, vulnerable SQLite native bundle resolution, local post-recording audio denoise export, installer proof readiness verification, clean-machine proof-kit generation, clean-machine evidence recording, required desktop evidence recording, manual desktop-proof summary generation, manual hardware-proof summary generation, hardware evidence recording, browser extension publication evidence recording, current browser optional-lane closure, governed plugin background update check/stage-only runs, plugin update Task Scheduler handoff generation, explicit plugin update Task Scheduler lifecycle commands, reviewed local-plugin activation metadata writes, browser extension enterprise policy template planning, OAuth/live-account proof planning, provider-specific OAuth proof-plan polish, OAuth live-evidence recording, Google Photos, YouTube, and OneNote executable OAuth share adapters, deterministic foreground mask generation, opt-in local companion portal media review, explicit self-hosted shared-token companion portal preview, external person-segmentation runner support, hosted person-segmentation service handoff, stage-only person-segmentation model package acquisition, still-image/video-frame mask quality evaluation, refreshed release proof bundles, and the refreshed 2026-06-27 manual-validation handoff/ADB timeout hardening. The current generated findings remain the practical unimplemented/proof-gap list: six release-blocking required human/clean-machine lanes, four hardware-gated claim boundaries, zero optional compatibility findings, and one parked live-provider lane.
